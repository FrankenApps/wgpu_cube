pub mod render {
    pub mod camera;
    pub mod camera_controller;
    pub mod geometry {
        pub mod r#box;
    }
    pub(crate) mod light;
    pub mod state;
    pub (crate) mod texture;
    pub (crate) mod vertex;
}